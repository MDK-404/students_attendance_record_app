class AdminCredentials {
  final String username;
  final String password;

  AdminCredentials(this.username, this.password);
   AdminCredentials adminCredentials = AdminCredentials('admin', 'admin');

}
